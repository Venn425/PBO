package PERTEMUAN_01;

import java.util.Scanner;

class hitung_vol{
	public float volkerucut(float jari, float tinggi) {
		return (float) (Math.PI * Math.pow(jari, 2) * tinggi / 3 );
	}
	public float voltabung(float jari, float tinggi) {
		return (float) (Math.PI * Math.pow(jari, 2) * tinggi);
	}
}

public class Latihan_02 {
	static hitung_vol volume = new hitung_vol();
	
	public static void main(String[] args) {
		System.out.println("Program Menghitung Volume");
		
		float jari, tinggi;
		Scanner input = new Scanner(System.in);
		
		System.out.print("Masukkan Jari - Jari: ");
		jari = input.nextFloat();
		
		System.out.print("Masukkan Tinggi: ");
		tinggi = input.nextFloat();
		
		float volTabung = volume.volkerucut(jari, tinggi);
		float volKerucut = volume.voltabung(jari, tinggi);
		
		System.out.println("Volume Kerucut: " + volKerucut);
		System.out.println("Volume Tabung: " + volTabung);
	}
}
